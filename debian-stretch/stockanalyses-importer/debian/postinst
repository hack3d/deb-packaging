#!/bin/sh
# postinst script for stockanalyses-importer
#
# see: dh_installdeb(1)

set -e

case "$1" in
	configure)
          
	# copy default config if it doesn't exist
        if [ ! -f "/opt/stockanalyses-importer/lib/python3.5/site-packages/downloader/config" ]
	then
            if [ -e "/opt/stockanalyses-importer/lib/python3.5/site-packages/importer/config.default" ]; then
                cp /opt/stockanalyses-importer/lib/python3.5/site-packages/importer/config.default /opt/stockanalyses-importer/lib/python3.5/site-packages/importer/config
            fi
        fi
            
	;;
	
	abort-upgrade|abort-remove|abort-deconfigure)
		exit 0
	;;

	*)
		echo "postinst called with unknown argument \`$1'" >&2
		exit 1
	;;

esac


# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#


exit 0